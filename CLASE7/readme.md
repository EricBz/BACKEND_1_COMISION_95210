# AGREGAMOS HANDLEBARS:

## EXPLICACION PASO A PASO:
- Configuramos hbs en la carpeta config, ahi esta su motor.
- importamos la funcion en app y mandamos esta misma como parametro (app).
- Creamos los hbs que se renderizaran como front.
- en la carpeta "public", creamos los archivos que luego necesitaremos y seran publicos
js del cliente, css.
- Recordar todo esto ponerlo en app, sus rutas y configuraciones para que la app sepa donde buscar cada cosa.
- Creamos ruta para views.
- Creamos controller para views, ya que ahora usaremos reder() como respuesta.
- Tomamos los mismos datos de antes de la capa service.
- Las otras carptas quedan igual.